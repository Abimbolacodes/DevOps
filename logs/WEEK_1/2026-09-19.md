What I did
- Made archive.sh executable and ran it
- Confirmed it created a timestamped archive file
- Committed and pushed the finished script to feature-script branch
- Solved the Contains Duplicate DSA problem

Commands I used
- `chmod +x archive.sh` — made the script executable
- `./archive.sh` — ran the script
- `ls` — confirmed the archive file was created
- `git add .`
- `git commit -m "Finish and test archive script"`
- `git push`

What I learned
- chmod +x is needed before a script can be run directly with ./
- How to confirm a script worked by checking for the expected output file
- How to use a set in Python to detect duplicates in one pass through a list

Code I wrote
```python
def contains_duplicate(nums):
    seen = set()
    for num in nums:
        if num in seen:
            return True
        seen.add(num)
    return False
```
